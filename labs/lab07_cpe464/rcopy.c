/*
 * rcopy (based on udpClient.c)
 *  - Must take in an error rate, read in user input, create an application level PDU,
 *    print that PDU, and then send (using sendtoErr()) the PDU to the server
 *
 * */

#include "rcopy.h"
